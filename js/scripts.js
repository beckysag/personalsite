$(function(){
/*
	// Page setup at initial page load	
	var target = window.location.hash;

	if (target.length > 0) {		
		// If no hashtag, load the main page
		loadContent(target.replace(/#/,''));
	} else {	
		// Else use hashtag to determine which page to load
		loadContent('home');
	}


	// Function to load content when user clicks a new menu button
	function loadContent(target){
		$.ajax({
			type: "POST",
			url: "select_content.php",
			data: { 
				target: target 
			}
		})	
		.done(function(msg) {
			$('#content').empty().append(msg);
		})
		.fail(function() {
			console.log("error");
		});
	}

	$('#main-nav a').click(function(){
		var target = $(this).attr('href').replace(/#/,'');
		loadContent(target);
	});
		

*/

}); // $.ready
